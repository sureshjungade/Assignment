package com.evalC2_1Que;

public class AccessModifiers {

	//explain about access modifiers and their accessibility?
	
	/*
	 * with the help of access modifiers we can access the methods varibles and constructors.
	 * access modifiers can be a variable, method and constructor also.
	 * in java we have a four type of access modifiers
	 * 1. public: we can access this modifiers from everywhere like from inside class, outside of class
	 * inside and outside of package.
	 * 
	 * 2. private: we can access this modifiers only in the package, we can't access this class or method
	 * outside of the package.
	 * 
	 * 3. default: it is same as protected modifier but we can't inherit it from outside the package.
	 * we don't have to give any name like private or public.
	 * 
	 * 4. protected: we can access this modifiers only in the package, if we want to access from outside 
	 * the package then we have to inherit that access modifiers class or methods.*/
	
	
	/*
	 * Explain the difference between IS-A and Has-A relationship
	 * if we inherits sedan class from car class then we can say car and sedan IS-A relationship of
	 * parent and child
	 * */

}
